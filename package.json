{
  "name": "frank-media",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix frontend run dev",
    "dev:api": "npm --prefix backend run dev",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "build": "npm --prefix frontend run build && npm --prefix backend run check"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
